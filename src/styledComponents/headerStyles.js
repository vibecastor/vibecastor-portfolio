import styled from 'styled-components';

const HeaderWrapper = styled.header`
  background: #1a237e;
  width: 100%;

  .bar {
    background: #000051;
  }

  img {
    border-radius: 50%;
  }
`;

export default HeaderWrapper;
